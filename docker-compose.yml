services:
  server:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ".:/site"

  summit-2026:
    image: bretfisher/jekyll-serve
    ports:
      - "4001:4000"
    volumes:
      - "./summit/2026:/site"
