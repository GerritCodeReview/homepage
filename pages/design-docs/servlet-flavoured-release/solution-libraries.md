---
title: ""
permalink: design-docs/servlet-flavoured-release-solution-libraries.html
hide_sidebar: true
hide_navtoggle: true
toc: false
---

# Library flavours

Each servlet-facing library provides both flavours from a single upstream source
tree — there is no fork — and a generated flavour keeps the original package
names:

* **JGit** is `jakarta.servlet`-canonical and owns a generated EE8
  (`javax.servlet`) module set, derived from those canonical sources by the
  shared, dependency-free bazlets transform.
* **Gitiles** is likewise `jakarta.servlet`-canonical as of `gitiles-servlet`
  2.0.0, and mirrors JGit's naming exactly: the unsuffixed `gitiles-servlet`
  artifact is the canonical jakarta library, and a class-for-class-identical
  EE8 twin — generated by the same bazlets transform, added under the
  [`jakarta-canonical`](https://gerrit-review.googlesource.com/q/topic:jakarta-canonical)
  topic — publishes as `gitiles-servlet-ee8` (the analogue of JGit's
  `org.eclipse.jgit.http.server.ee8`). EE8 consumers thereby keep tracking
  current Gitiles code; the prior `javax.servlet` release line (1.6.x) is
  frozen.

Because a generated flavour keeps the same class names as its canonical
counterpart, only one of the pair may sit on a given classpath — which is why
each WAR carries exactly one flavour. Duplication stays at the release boundary;
the source and maintenance model is shared. Both bridges are transitional:
their retirement is prepared under the
[`drop-ee8-bridges`](https://gerrit-review.googlesource.com/q/topic:drop-ee8-bridges)
topic and lands once their javax consumers have migrated.

The JGit side is specified in depth in the
[JGit EE8 Servlet Bridge design proposal](https://github.com/davido/jgit-ee8-servlet-bridge-design-proposal),
which covers the first-class EE8 module design and the same-FQDN / no-p2
constraint.
